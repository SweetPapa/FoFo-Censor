ROLE: Audio disambiguator (design §9, batched).

For each candidate word (with its surrounding line), decide whether it is
objectionable in context. Homographs like "ass" (insult vs. animal) depend on
context. Slurs are ALWAYS objectionable — never un-flag them. Also mark whether
the word is meaning-bearing (removing it would damage the story's meaning).

Respond with JSON only, an array aligned to the input:
[ { "word_id": "...", "is_objectionable": true, "category": "profanity|slur|sexual_term", "tier": "mild|moderate|severe", "meaning_bearing": false } ]
