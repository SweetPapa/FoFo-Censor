"""Textual app entry point (design §11a). STUB."""

from __future__ import annotations

from typing import Optional


def run_tui(map_path: Optional[str] = None) -> int:
    """Launch the review TUI. Not implemented yet."""
    raise NotImplementedError(
        "The review TUI (M2/§11a) is not implemented yet. Build with Textual: "
        "profile picker, staged progress, visual-edit review, coverage report."
    )
